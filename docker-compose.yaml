version: '2'

services:
  monitor:
    image: 'data-feed-status-bot'
    environment:
      TOKEN: $TOKEN
      CHANNEL_ID: $CHANNEL_ID
      POLLING_INTERVAL: $POLLING_INTERVAL
      FEED_EXPLORER_API: $FEED_EXPLORER_API
